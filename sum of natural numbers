#include <stdio.h>
int main()
{
    int n, i, total = 0;
    
    printf("Enter a positive integer: ");
    scanf("%d",&n);

    for(i=1; i <= n; ++i)
    {
        total += i;   
    }

    printf("total = %d",total);

    return 0;
}
